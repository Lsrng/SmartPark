<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartpark.mapper.EnterpriseConfigVersionMapper">

    <select id="selectCurrentVersion" resultType="java.lang.Integer">
        SELECT current_version
        FROM enterprise_config_version
        WHERE type_id = #{typeId}
    </select>

    <update id="updateVersion">
        INSERT INTO enterprise_config_version (type_id, current_version)
        VALUES (#{typeId}, #{version})
        ON DUPLICATE KEY UPDATE current_version = #{version}, updated_at = NOW()
    </update>

</mapper>
